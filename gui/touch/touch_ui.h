/* gui/touch/touch_ui.h — тач-оболочка proshivkaOS NEXT.
 * Аналог gui/wm.c для десктопной сборки: точка входа графической системы,
 * из неё не возвращаются.
 */
#ifndef GUI_TOUCH_UI_H
#define GUI_TOUCH_UI_H

void touch_main(void);

#endif
